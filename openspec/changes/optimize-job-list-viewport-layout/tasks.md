## 1. 满视口列表布局

- [x] 1.1 将 `/jobs` 根容器改为动态满视口纵向布局，并限制文档级滚动
- [x] 1.2 让加载、空态、错误态和招聘表格正确占用中间剩余空间
- [x] 1.3 将招聘表格改为隐藏滚动条的独立双轴滚动区，并固定表头
- [x] 1.4 保持分页栏位于视口底部且不覆盖表格内容

## 2. 页面导航

- [x] 2.1 将“返回简历”改为带图标的“我的简历”入口
- [x] 2.2 新增带图标的“投递管理”入口并跳转 `/applications`
- [x] 2.3 调整页头操作区的窄屏换行行为，避免按钮文字重叠或截断

## 3. 验证

- [x] 3.1 运行前端生产构建与 OpenSpec 严格校验
- [x] 3.2 在桌面视口验证表格铺满、内部滚动、固定表头和常驻分页
- [x] 3.3 在 375px 窄屏验证导航、表格横向触控滚动和分页无重叠
- [x] 3.4 验证“我的简历”和“投递管理”按钮跳转到正确页面

## 4. 顶部筛选工具栏优化

- [x] 4.1 将 `JobFilterBar` 改为无独立背景和边框的紧凑响应式控件组
- [x] 4.2 将筛选控件移入 `/jobs` 页头并移除说明文案及独立筛选占位
- [x] 4.3 保持顶部重复结果计数已移除，并清理无效 `total` prop
- [x] 4.4 重新构建并验证 1280px 与 375px 下列表可用高度、控件换行和分页可见性

## 5. 筛选交互与列表视觉

- [x] 5.1 为 `StyledSelect` 增加可选模态配置，并将招聘页两个筛选设为非模态
- [x] 5.2 调整表格标题栏和底部分页的中性色、文字与圆角层级
- [x] 5.3 在浏览器中验证行业与类型筛选可双向单击直接切换

## 6. 自动同步

- [x] 6.1 删除 `/jobs` 手动同步按钮、冷却状态和无效依赖
- [x] 6.2 将服务端默认同步周期改为一小时，并补充 Compose 与示例环境变量
- [x] 6.3 为调度器默认周期补充 Go 测试
- [x] 6.4 为长时间打开的 `/jobs` 页面增加每小时列表与筛选刷新

## 7. 最终验证

- [x] 7.1 运行前端生产构建、相关 Go 测试、OpenSpec 严格校验和 diff 检查
- [x] 7.2 在桌面和移动视口复核表头、分页、页面高度与控件无重叠

## 8. 表头颜色微调

- [x] 8.1 将表头蓝灰色调整为纯中性浅灰色
- [x] 8.2 运行前端构建、OpenSpec 严格校验并在浏览器核验实际颜色
