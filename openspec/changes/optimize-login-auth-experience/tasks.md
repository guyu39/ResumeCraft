## 1. 认证优先布局

- [x] 1.1 将登录页改为单一响应式布局，删除桌面双相位状态、透明隐藏区域和滚轮接管
- [x] 1.2 将产品介绍压缩为三个真实闭环，并在移动端将认证表单排在完整能力说明之前
- [x] 1.3 移除渐变光斑、漂浮装饰、多层功能卡和不再使用的登录页动画样式

## 2. 表单语义与校验

- [x] 2.1 为姓名、邮箱、密码、确认密码和验证码补齐字段名称、标签、自动填充与 ARIA 错误关联
- [x] 2.2 将空值、格式、长度和确认密码错误统一为字段级提示，并在提交失败时聚焦首个无效字段
- [x] 2.3 为密码与确认密码增加独立的显示/隐藏按钮
- [x] 2.4 为登录方式补齐标签组语义，并将加载文案细化为登录或注册动作

## 3. 行为回归与验证

- [x] 3.1 验证密码登录、验证码登录、验证码注册、会话失效提示、顶号确认和安全回跳逻辑保持不变
- [x] 3.2 运行前端生产构建、OpenSpec 严格校验和 diff 检查
- [x] 3.3 在 1280x800 桌面视口验证单屏布局、表单操作和键盘 Tab 顺序
- [x] 3.4 在 375x812 移动视口验证表单优先、原生滚动和控件无重叠

## 4. 半透明视觉与错误气泡

- [x] 4.1 将字段错误改为标题行右侧的消息气泡，并保持 ARIA 错误关联
- [x] 4.2 为页面增加柔和背景色，将认证容器、输入框和三张能力卡调整为半透明样式
- [x] 4.3 为能力卡增加轻量悬停动画，并兼容减少动态效果偏好
- [x] 4.4 运行构建与 OpenSpec 校验，在桌面和移动视口验证气泡、透明度、悬停和溢出

## 5. 注册态桌面高度

- [x] 5.1 压缩注册态卡片内边距、表单字段间距和底部切换间距，保持输入框 44px 高度
- [x] 5.2 缩小桌面页面垂直安全边距，确保注册态不产生文档级滚动或表单内部滚动
- [x] 5.3 在 1280x720、1024x640 和 375x812 视口验证注册表单高度与滚动行为
