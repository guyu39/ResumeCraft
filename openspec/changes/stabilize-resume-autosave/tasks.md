## 1. 持久化契约与版本元数据

- [x] 1.1 扩展前后端简历更新请求，补齐 `locale`、`template` 和完整持久化载荷字段，并保持旧客户端字段兼容。
- [x] 1.2 定义客户端 `localRevision`、`ackedRevision`、`serverVersion` 的存储结构和迁移默认值。
- [x] 1.3 统一完整持久化载荷序列化与 hash 计算，覆盖正文、个人信息、当前快照关联和快照草稿。
- [x] 1.4 在后端单事务中更新 content JSON 与独立列，正确递增/校验 content 和 snapshot drafts 两个版本域。

## 2. 前端本地草稿与云端保存队列

- [x] 2.1 改造 resumeStore，使持久化字段变更先写入本地草稿并递增 local revision。
- [x] 2.2 在 useCloudSync 中实现单简历串行保存队列，保存期间出现新 revision 时自动补保存最新 payload。
- [x] 2.3 将重复触发合并为同一 revision 保存，避免重复 PUT；确保保存请求固定 payload，不使用陈旧 closure。
- [x] 2.4 让保存成功只确认实际提交请求对应的 revision，并同步 serverVersion、ackedRevision 和 draftsVersion。
- [x] 2.5 将 keepalive 降级为退出兜底，不在无法读取响应时标记云端已确认。

## 3. 触发、恢复与失败处理

- [x] 3.1 为输入防抖、失焦、模块结构变化、快照切换、切后台、导出和 AI/JD 操作接入统一 flush 入口。
- [x] 3.2 让导出和 AI/JD 调用方检查 flush 结果，区分“云端已确认”和“仅本地保留”。
- [x] 3.3 增加有限指数退避重试和 `online` 事件恢复同步，保留失败 revision。
- [x] 3.4 重写启动恢复逻辑，使用 revision/确认状态判断本地草稿，不再直接比较客户端 savedAt 与服务端 updatedAt。
- [x] 3.5 实现 409 自动处理：内容一致时对齐版本，本地有未确认 revision 时重试，本地干净时采用云端。

## 4. 保存状态反馈

- [x] 4.1 在编辑器顶部或现有工作区通知区域增加编辑中、保存中、已自动保存、本地待同步、失败和离线状态。
- [x] 4.2 增加可访问的状态播报和失败重试入口，成功状态不使用打断式 Toast。
- [ ] 4.3 在桌面和窄屏布局验证状态区域不遮挡编辑器、预览区或现有操作。

## 5. 测试与观测

- [ ] 5.1 编写 store/sync 测试：连续编辑、保存中再次编辑、重复触发、local revision 恢复和完整载荷 hash。
- [ ] 5.2 编写后端测试：locale/template 持久化、事务更新、版本 CAS、快照草稿版本和 409 行为。
- [ ] 5.3 编写集成场景：断网重试、online 恢复、切后台、keepalive 不误报、快照切换和 AI/导出前 flush。
- [ ] 5.4 增加保存成功率、409 次数、重试次数、请求体大小和恢复结果的结构化日志/指标；不记录简历正文和敏感个人信息。

## 6. 验证与 Redis 评估门禁

- [x] 6.1 运行前端构建、相关 Go 测试和 `git diff --check`。
- [ ] 6.2 使用浏览器核验保存状态在桌面/移动宽度下的显示和交互状态。
- [x] 6.3 在没有 Redis 的环境验证完整保存链路可用，确认 Redis 不是一期部署依赖。
- [ ] 6.4 根据请求体、耗时、吞吐和失败指标决定是否单独提出 Redis 队列/限流变更；未达到阈值时不引入 Redis。
