## 1. 数据库与契约

- [x] 1.1 新增幂等迁移，去除 `job_applications.snapshot_version_id` 的 NOT NULL 约束且不新增外键。
- [x] 1.2 同步更新 `init.sql` 中投递快照字段的可空定义。
- [x] 1.3 将后端和前端投递请求/响应类型改为可选快照，并定义显式清空语义。

## 2. 后端测试与实现

- [x] 2.1 增加 Service 测试，覆盖创建时省略快照、AI 来源为空和非法必填字段。
- [x] 2.2 增加 Repository 查询契约测试或可执行测试，覆盖 NULL 扫描、创建、绑定、清除及更换简历清空关联。
- [x] 2.3 修改创建链路：仅校验简历必填，快照非空时再校验归属和类型。
- [x] 2.4 修改更新链路，实现未提供/空值/非空值三态和更换简历清空旧关联。
- [x] 2.5 修改列表、详情、重复查询、删除、AI 来源和导出逻辑以兼容 NULL。
- [x] 2.6 修改按快照转化统计，将 NULL 聚合为“未关联版本”。

## 3. 前端交互

- [x] 3.1 移除快照必填校验和自动选择第一个快照的行为。
- [x] 3.2 在快照选择器中提供“暂不关联版本”，并允许没有候选快照时正常保存。
- [x] 3.3 编辑未关联记录时保持空值，更换简历时清空旧快照，分析跳转省略空 snapshotId。
- [x] 3.4 更新投递弹窗文案并保持现有响应式布局与可访问性。

## 4. 验证与交付

- [x] 4.1 运行 Go 格式化、后端相关测试和全量测试。
- [x] 4.2 运行前端生产构建和静态检查。
- [x] 4.3 运行 OpenSpec strict 校验和 `git diff --check`。
- [ ] 4.4 使用浏览器验证新增、编辑、绑定、清除及无快照简历保存流程。
